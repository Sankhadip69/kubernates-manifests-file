apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bookdelivery-db-pvc
spec:
  volumeName: bookdelivery-db-pv
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 4Gi
